{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-extension",
      "type": "shell",
      "command": "cmd.exe",
      "args": [
        "/d",
        "/c",
        "npm.cmd rebuild esbuild && npm.cmd run build"
      ],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "problemMatcher": []
    }
  ]
}
