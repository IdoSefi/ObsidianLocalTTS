## Severity
- P0 critical
- P1 major
- P2 normal
- P3 minor

## Status
- OPEN
- IN_PROGRESS
- FIXED
- WONT_FIX

| ID | Severity | Status | Summary | Repro Steps | Suspected Area | Notes |
|---|---|---|---|---|---|---|
| B-001 | P2 | WONT_FIX | Click-to-sentence mapping can be off in complex rendered Markdown layouts | Create a note with callouts/tables/footnotes; synthesize in Reading view; click inside nested rendered content and observe occasional wrong restart sentence | `plugin/src/sentence/mapping.ts` | Reading-mode click restart was removed in T-035; feature deferred |
| B-002 | P3 | OPEN | `/synthesize` fails if `voice` uses an unsupported Kokoro language prefix | Configure voice to a name whose first character is not a valid `KPipeline(lang_code=...)`; run synthesize | `server/app.py` | v1 derives language code from first letter of voice for pipeline selection |
| B-008 | P2 | FIXED | Cached sentence index mapping can drift after note edits, so replay may skip or mismatch some sentences | Synthesize note, edit sentence structure (insert/remove/reorder), run Play from cache without re-synthesizing | `plugin/src/main.ts`, `plugin/src/audio/cache.ts` | Fixed by validating sentence text hashes in both Play + Source-mode restart flows and re-synthesizing stale sentences from first mismatch onward (while skipping unnecessary full re-synthesis when only note-level hash differs) |
| B-011 | P3 | OPEN | In Source mode, right-clicking does not necessarily move the cursor before selecting "Start reading from here" | Place cursor in one sentence, right-click a different sentence, choose menu action, playback may start from old cursor location | `plugin/src/view/sourceModeHooks.ts`, `plugin/src/main.ts` | v1.2 avoids DOM offset logic and uses editor cursor offset only |
| B-012 | P1 | FIXED | After switching notes, Source-mode "Start reading from here" could restart playback from previously active note sentences | Start playback on note A, switch to note B, use Source-mode context menu action, prior build could continue note A playback context | `plugin/src/main.ts` | Fixed by tracking `sentencesNotePath` and reloading note-B cache sentences before restart |
| B-013 | P3 | OPEN | Double-arrow sentence shortcuts also allow normal cursor/navigation movement because key handling is passive by design | While playback is active, press `ArrowLeft` or `ArrowRight` twice quickly; sentence jump triggers on second press while the key still performs default Obsidian navigation behavior | `plugin/src/main.ts` | Expected v1.2 tradeoff for robustness and minimal key interception; avoids aggressive `preventDefault()` |

| B-009 | P1 | FIXED | Synthesis failed when outputDir pointed to vault path due server-side temp-root enforcement | Trigger synth after vault-cache migration; server returned "outputDir must be under system temp" | `plugin/src/main.ts`, `plugin/src/audio/cache.ts`, `server/app.py` | Fixed by introducing temp staging output directory and copying successful sentence WAVs into persistent vault cache |

| B-010 | P1 | FIXED | Playback failed with `ERR_FILE_NOT_FOUND` because newly-created Blob URLs were revoked before `Audio` playback began | Synthesize note, then autoplay/replay; observe blob resource load failures and "Could not start playback" notices | `plugin/src/audio/playback.ts` | Fixed by removing redundant cleanup call that revoked the current object URL immediately before constructing `Audio` |

| B-003 | P1 | FIXED | Desktop fetch transport failed before `/synthesize`, causing false “Could not reach local server” notices | Run server locally, confirm `/health` works, trigger synth in Obsidian; old build only showed health and never posted synth | `plugin/src/audio/kokoroClient.ts` and `plugin/src/main.ts` | Fixed by moving localhost calls to Obsidian `requestUrl()` and surfacing exact transport/HTTP/server errors |
| B-004 | P1 | FIXED | `/synthesize` could return ok=true while writing effectively silent/invalid WAV output | Run synthesis where model yields silent/all-zero/near-zero/non-finite chunks; prior server still wrote WAV | `server/app.py` | Fixed by chunk/final waveform validation and explicit API errors; added `server/debug_kokoro_direct.py` and upgraded `server/test_synthesize_request.py` to verify audibility |
| B-005 | P2 | FIXED | `server/debug_kokoro_direct.py` failed on Windows with `FileNotFoundError` for `\tmp\kokoro-direct-debug.wav` | Run script on Windows; synth succeeds but write step fails because hardcoded POSIX path is invalid | `server/debug_kokoro_direct.py` | Fixed by using `tempfile.gettempdir()` and creating parent directories before writing WAV |
| B-006 | P2 | FIXED | Playback/synthesis had low visibility, making successful synthesis and active playback unclear | Synthesize a note and observe little/no UI feedback in prior build | `plugin/src/main.ts`, `plugin/src/ui/status.ts`, `plugin/src/audio/playback.ts` | Fixed by synthesis progress notices, status bar states, playback progress slider, and missing-audio notices |
| B-007 | P1 | FIXED | Playback failed with "Not allowed to load local resource" for `file:///.../Temp/...wav` on desktop | Synthesize successfully, then autoplay from temp WAV path on Windows; prior build attempted direct `file://` audio URL and playback never started | `plugin/src/audio/playback.ts` | Fixed by loading WAV bytes from disk and playing via Blob object URL (`URL.createObjectURL`) with URL cleanup on stop |
| B-014 | P1 | FIXED | Sentence jump / Source restart could trigger stale media-element errors (`Empty src attribute`) and desynchronize play/pause + sentence navigation state | Pause playback, use previous/next or Source-mode "Start reading from here"; prior build could start audio but show Play/disabled nav, then fail subsequent jumps | `plugin/src/audio/playback.ts`, `plugin/src/main.ts`, `plugin/src/ui/status.ts` | Fixed by active-audio run/instance guards, listener-detach teardown (no `src=""` reset), and relying on controller state for pause/resume behavior |
| B-015 | P2 | FIXED | Source mode had no robust current-sentence highlight during playback | Start playback in Source mode and observe no sentence highlight, or stale highlight behavior across stop/fail/note switches | `plugin/src/main.ts`, `plugin/src/view/sourceModeHighlight.ts` | Fixed by CM6 `StateField<DecorationSet>` highlighting plus tracked CM6 editor views (`ViewPlugin`) and `editorInfoField` note-path matching so effects dispatch to the correct active source editor |
| B-016 | P0 | FIXED | Opening notes could crash editor with "Unrecognized extension value in extension set" after Source-highlight changes | Enable plugin build that imports CM6 packages from bundled copy; click any note; Obsidian throws extension-set error and note fails to open | `plugin/esbuild.config.mjs`, `plugin/src/view/sourceModeHighlight.ts` | Fixed by externalizing `@codemirror/state` and `@codemirror/view` in esbuild so plugin uses Obsidian's CM6 module instances (no duplicate-instance `instanceof` mismatch) |
| B-017 | P1 | FIXED | Next-sentence jump during active synthesis could fail with "Could not start playback... Audio path: unknown path" for not-yet-ready target sentence | Start synthesis/playback, click Next to a sentence not synthesized yet, observe failed state/error log and playback exit | `plugin/src/main.ts`, `plugin/src/audio/playback.ts` | Fixed by treating non-ready/non-error target sentence as pending when waiting is allowed: suppress failed-state emission and suppress plugin-level hard error notice while synthesis is still in progress |
| B-018 | P3 | FIXED | No user feedback was shown while playback waited for a not-yet-synthesized target sentence | During active synthesis, use next/previous/start-from-here or let playback auto-advance into a pending sentence; prior build waited silently | `plugin/src/audio/playback.ts`, `plugin/src/main.ts` | Fixed by adding a one-time-per-wait callback from playback controller and surfacing a Notice: waiting for sentence X/Y synthesis |
| B-019 | P1 | FIXED | TTS spoke Markdown syntax and could split sentences incorrectly on file extensions inside paths/filenames | Synthesize text containing Markdown markers (e.g., `**bold**`, links, wikilinks) and file paths like `/tmp/my.file.txt`; prior build could speak symbols and split around `.txt` | `plugin/src/sentence/splitter.ts`, `plugin/src/sentence/pathMasking.ts`, `plugin/src/sentence/normalize.ts`, `plugin/src/main.ts` | Fixed by same-length path-dot masking for sentence detection plus per-sentence spoken-text normalization (Markdown cleanup + basename-only path speech) while preserving raw offsets for Source-mode features |
| B-020 | P3 | OPEN | Piper speed control is approximated through CLI `--length_scale` inversion and may not match Kokoro pacing semantics exactly | Switch backend to Piper and compare speech tempo at same plugin speed setting across backends | `server/app.py` | Expected tradeoff in initial Piper support; plugin keeps one shared `speed` setting across backends |
| B-021 | P3 | FIXED | Cache root folder was visible as `audio_synthesis/`, cluttering vault navigation | Synthesize any note and inspect vault file list; old builds created a top-level visible cache folder | `plugin/src/audio/cache.ts` | Fixed by moving cache root to hidden `.audio_synthesis/` |
| B-022 | P3 | FIXED | README setup flow was too scattered and missed a clear initialization order for Windows Piper users | Follow docs from fresh checkout on Windows and attempt Piper setup; prior README required piecing steps together | `README.md` | Fixed by rewriting setup into ordered install/init flow with explicit PowerShell env commands |
| B-023 | P1 | FIXED | Rapid control presses during synthesis/wait states could queue stale commands that fired later, and Stop did not fully cancel delayed continuations | While synthesis or wait-for-ready is active, press Next/Previous/Play/Restart repeatedly; old builds could execute delayed stale actions even after Stop | `plugin/src/main.ts` | Fixed by plugin-level command/synthesis run supersession checks after async boundaries plus global Stop invalidation of command/synthesis runs and playback waits |
| B-024 | P1 | FIXED | Using Source-mode "Start reading from here" during active synthesis could fail early with "Could not start playback ... unknown path" instead of waiting for target sentence readiness | Start synth+play, right-click a later unsynthesized sentence in Source mode, select "Start reading from here" | `plugin/src/main.ts` | Fixed by ensuring command supersession no longer cancels active synthesis (except explicit Stop), preserving wait-for-ready behavior for pending target sentences |
| B-025 | P1 | FIXED | Cache reload during active synthesis could still mark pending later sentences as `error`, causing "Could not start playback ... unknown path" on Source-mode restart | Start synthesis, then trigger cache-backed restart/play on a later not-yet-ready sentence while synthesis remains active | `plugin/src/main.ts` | Fixed by preserving existing in-memory sentence states (`generating`/`ready`) during cache reload for the active synthesizing note, instead of downgrading missing cache files to hard error |
| B-026 | P1 | FIXED | Playback and Source-mode restart required manual pre-synthesis and could not synthesize only the needed suffix on demand | Press Play or use Source-mode `Start reading from here` on notes with missing/stale cache; prior flow depended on explicit synth action and broad cache-first assumptions | `plugin/src/main.ts`, `plugin/src/ui/controls.ts` | Fixed by removing explicit synth UI and adding start-index-aware playback preparation that validates cache, synthesizes only required suffix sentences, and auto-starts playback once target sentence audio is ready |
| B-027 | P1 | FIXED | Concurrent/on-overlap synthesis runs could delete shared temp staging outputs, causing server-side `No such file or directory ... sentence-xxxx.wav` failures and leaving broken sentence cache entries | Trigger multiple play/restart actions while synthesis is active; old staging path was reused and cleaned with broad folder deletes, producing intermittent missing sentence output paths and later playback holes | `plugin/src/audio/cache.ts`, `plugin/src/main.ts` | Fixed by allocating unique per-run temp staging directories (`mkdtemp`) and deleting only that run folder; playback prep now also treats missing/non-WAV cached files as unusable and re-synthesizes them automatically |
